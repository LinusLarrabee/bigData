# 模块

## 开发流程

|          |                                                      | 预计时间 | 当前进展 |
| -------- | ---------------------------------------------------- | -------- | -------- |
| TAUC-001 | 将Network的代码迁到Wifi-Management                   | 8d       | 3d/8d    |
| TAUC-002 | 将Tpuc-eventfrontend的代码切到Wifi-Management        | 0.5d     |          |
| TAUC-003 | 构建Network和Wifi-manegement的通信                   | 1d       |          |
| TAUC-004 | 将Wifi-Management原有代码进行适配，相同DTO只保留一份 | 1d       |          |
| TAUC-005 | 迁移校验系统开发                                     | 2d       |          |
| TAUC-006 | Apollo开关配置                                       | 0.5d     |          |
| TAUC-007 | S3原始数据重写逻辑                                   | 0.5d     |          |
| TAUC-008 | 幂等性校验                                           | 0.5d     |          |
| TAUC-009 | 业务逻辑修改                                         | 4d       |          |

## 涉及微服务

tpuc-eventFrontend

tpuc-wifi-manegement

tauc-network





# 细节

## 1.trId问题

原始Qoe分数计算涉及trId，deviceId，networkId相互

```
Tr交互
public class WirelessGrpcService
public GetWirelessPrimaryInfoResp getWirelessInfo(GetWirelessPrimaryInfoReq request)
usage: reply.getResult().getBandSteeringEnable()

public class TrIdCache
public String getDeviceIdByTrId(String firstControllerTrId)
public String getTrIdByDeviceId(String deviceId)

涉及代码
boolean bandSteeringEnable = true;
Optional<IspNetworkDeviceDO> ispNetworkDeviceDO = ispNetworkDeviceDao.findByMac(deviceMac);
if (ispNetworkDeviceDO.isPresent()) {
    Response<TrWirelessInfo> trWirelessInfoResponse = getTrWirelessPrimaryInfo(
            ispNetworkDeviceDO.get().getDeviceId().getDeviceId(), ispNetworkDeviceDO.get().getIspAdminId().getId(), false);
    if (trWirelessInfoResponse.isOk()) {
        bandSteeringEnable = trWirelessInfoResponse.getResult().getBandSteeringEnable();
    }
}

处理方案：
1. 提供上述所有接口
2. 提供 Response<TrWirelessInfo> getTrWirelessPrimaryInfo(String deviceId, String ispAdminId, Boolean refresh) 的简化版本，输出getBandSteeringEnable即可。
3. 输入mac，输出getBandSteeringEnable

```





case2

鉴权操作

```
eventfrontend
  rpc CheckDevice (CheckDeviceReq) returns (CheckDeviceResp)
  
wifi-management
   CheckReportPermissionResult checkTheDataReportingSourceAndVasPermission(String firstControllerTrId)


public class CheckReportPermissionResult {

    IspNetworkDeviceDO ispNetworkDeviceDO;
    CheckVasLicenseResultDTO checkVasLicenseResultDTO;
    boolean checkResult;

}

建议：两次鉴权移成一次
    public List<IspNetworkDeviceDO> findByDeviceId(DeviceId deviceId) {
        return null;
    }
这个也可以被取缔
```



优化设计：减少对network调用，可以将鉴权全部放在eventfrontend前面。

## 2. Activity

本部分写入了mongodb，可以把这个写表和对应读表操作放到wifi-management

```java
Response<Void> saveAlert(@RequestBody AlertSaveDTO alertSaveDTO);
```

规划上：本期先不考虑，本期优先考虑和Network交互部分



## 3.展示部分

```E
        // 获取此网络下的所有client 的mac
        Response<List<TrClientVO>> response = new Response<>();
        try {
            response = getTrClientListService.getClientListNetworkHealth(
                ispNetworkDeviceDO.getDeviceId().getDeviceId());
        } catch (Exception e) {
            log.info("fail to getClientListNetworkHealth", e);
        }
        if (response.isError()) {
            return Response.error(ResponseCode.DEVICE_NOT_FOUND);
        }
        Set<String> networkClientMacs = new HashSet<>();
        if (CollectionUtils.isNotEmpty(response.getResult())) {
            networkClientMacs = response.getResult().stream().map(trClientVO -> {
                return trClientVO.getMac().replace(":", "");
            }).collect(Collectors.toSet());
        }
```

eg，比如需要找所有client的mac地址。



然后当前quality的表的查询逻辑是需要先networkid再mac再time，而接口上来的是mac，因此这些如果要迁移则需要大量findnetworkidbymac



比如

public Response<BandwidthVO> getAvgIspOfferedBandwidthIn30Days(String mac) {

有大量的network接口。
