# 一、系统架构

### 原始架构

缺陷：

1. 现有大数据部计算工具时延较长。
2. QoE模块位于Tauc-network，需进行拆分。

![image-20240825111123788](img/posts/24-08-26数据采集评审.asserts/image-20240825111123788.png)



### 数据采集评审设计

新增项：

1. tpuc-event-frontend新增数据链路，原始数据在Controller层完成鉴权后直接通过kafka进行中转
   1. tauc-data-collector鉴权	
      1. Headers里字段origin为指定值（dev/uat无此限制）
      2. 请求体字段uvi/sr/accountId非空
   2. tpuc-event-frontend鉴权
      1. 使用请求头的oui-sn-pc字段（即trId）进行鉴权
2. tauc-data-collector新增kafka消费模块，数据每拉取指定条目会上报一次S3，指定K小时后会将当前拉的数据一次上传。
   1. 目前配置的是拉取200条上传一次，第二天上报前一天消息

![image-20240825111401665](img/posts/24-08-26数据采集评审.asserts/image-20240825111401665.png)



### QoE迁移

迁移项：

1. 将原有Tpuc-event-frontend的解析部分迁移到tpuc-wifi-management
2. 将原有Tauc-Network的QoE分数计算部分（包括和其他微服务间的调用）迁移到tpuc-wifi-management
3. 将原有Tauc-Network和Aggweb之间的通信部分迁移到tpuc-wifi-management

计划新增项（TBC）：

1. 迁移比对：比对同一kafka消费发送的数据是否一致。
2. 幂等性校验：校验一条消息是否被消费两次（适配未来shd-eventCenter）。
3. 消息重发：使用保存到S3的数据重新发送进行kafka消费（确保消息被传达）。

![image-20240825112837540](img/posts/24-08-26数据采集评审.asserts/image-20240825112837540.png)

# 二、资源申请

### Kafka（已申请）

uat

tauc.qoe.raw.data

pet

tauc.qoe.raw.data.pet

prd

tauc.qoe.raw.data



### S3

存储设计：

命名（三区部署）：

prd-tauc-aps1-data-analysis

prd-tauc-use1-data-analysis

prd-tauc-euw1-data-analysis

文件会存放到/source目录下



规则：

Manage Lifecycle，将路径/source的生命周期设置为7天。



