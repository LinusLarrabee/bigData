google的wrapper待测试

Proto之间的依赖关系目前只支持同文件夹下的依赖



数据上报中转到数据部的错误码问题：这个是在pet测试过程中发现并修复的，并未提bug。

跨域问题是uat和staging本身的配置就不同，提测uat时未配置跨域校验便未交代该部分内容。

而之前并不知悉staging非bug上的改动需要通知到测试，再加上pet测试耗时较久，因此告知测试此项改动时已经很晚了。

限流问题当时只考虑了对基础云部分的限流，





1. yapi页面新建目录
2. data-collector dev环境的配置修改



1. 理论和实际代码的演示
2. 数据组件无spark，而且近些年qcon会议未提到
