功能1：GRPC测试工具：

- 对于服务端，根据其提供服务的proto文件，设计工具对该服务进行测试

功能2：Mock服务

提供客户端所需的服务端对应的Proto文件，自动生成对应的Mock服务，该服务需要对客户端传来的rpc信息进行响应



经过调研，目前实现gRPC接口测试功能的方案采用底层package：io.grpc.reflection.v1alpha

目前Postman的Grpc的功能较为齐全，提供proto解析与根据reflection两种方式对指定服务进行测试。



对于功能2：

首先使用protoc对proto解析基本的类与grpc通信模块。

其次生成服务端的实现。

对于proto文件的内容，可以将其视为三部分，head，service，message&enum



设计方案：



